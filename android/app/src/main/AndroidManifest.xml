<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pixeldrive.emulator">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Graphics Hardware Features -->
    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:version="1"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <!-- Touchscreen requirement -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="true" />

    <application
        android:label="@string/app_name"
        android:hasCode="false"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixelDrive">

        <!-- NativeActivity Host -->
        <activity
            android:name="android.app.NativeActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|navigation|uiMode"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="false"
            android:exported="true">

            <!-- Tell NativeActivity which dynamic library to load (.so) -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="pixeldrive" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
